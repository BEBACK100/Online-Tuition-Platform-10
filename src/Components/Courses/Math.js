import React from 'react';
import Navbar from '../Navbar/Navbar';

const Math = () => {
    return (
        <div>
            <Navbar></Navbar>
            <h1>This is the Math page</h1>
        </div>
    );
};

export default Math;