import React from 'react';
import Navbar from '../Navbar/Navbar';

const Physics = () => {
    return (
        <div>
            <Navbar></Navbar>
            <h1>Thsi is the physics page</h1>
        </div>
    );
};

export default Physics;